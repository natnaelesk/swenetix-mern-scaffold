# Client

Vite + React, **plain JavaScript** (no TypeScript).

Create the Vite app in this folder when you start Section 0, or replace this stub with `npm create vite@latest . -- --template react`.

Follow `../ROADMAP.md`. Keep UI simple.
